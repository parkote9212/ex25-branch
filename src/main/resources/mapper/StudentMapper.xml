<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pgc.mybatis.mapper.StudentMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO student (name, email, age, created_at, updated_at)
        VALUES (#{name}, #{email}, #{age}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findById" resultType="com.pgc.mybatis.domain.Student">
        SELECT *
        FROM student
        WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.pgc.mybatis.domain.Student">
        SELECT *
        FROM student
        ORDER BY id DESC
    </select>

    <update id="update">
        UPDATE student
        SET name       = #{name},
            email      = #{email},
            age        = #{age},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE
        FROM student
        WHERE id = #{id}
    </delete>

</mapper>